// Intentionally empty. Tailwind v4 is wired in via the @tailwindcss/vite
// plugin (see vite.config.js), which handles CSS processing directly and
// does not go through a PostCSS config file. Keeping this file present
// but empty (rather than deleted) avoids Vite falling back to stale
// PostCSS plugin references.
export default {
  plugins: {},
};
